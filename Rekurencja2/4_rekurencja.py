"""Dana jest tablica T[N]. Proszę napisać funkcję, która znajdzie niepusty, najmniejszy (w sensie
liczebności, oraz sumy) podzbiór elementów tablicy, dla którego suma elementów jest równa sumie indeksów tych elementów. Do funkcji należy przekazać tablicę, funkcja powinna zwrócić krotkę (sumę elementów, ilość) znalezionego podzbioru.
Na przykład dla tablicy: [ 1,7,3,5,11,2 ] rozwiązaniem jest liczba 8."""

from Test import Test
from test_file import args_4

def zad(tab, indeks=0, suma_ind=0, suma_ele=0, curr_ile=0,before = None):
    return
Test(zad, args_4).runtest()

"""Poprzednie zadanie, ale odważniki można umieszczać na obu szalkach."""

def zad_dod(T, i, r):
    return